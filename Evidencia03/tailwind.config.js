/** @type {import('tailwindcss').Config} */
// Define el tipo de config q usará Tailwind.

module.exports = {
  darkMode: 'class', 
 
  content: ["./index.html", "./src/**/*.{js,jsx}"], 
  
  theme: {
    extend: {}, 
   
  },

  plugins: [], 
 
};

// Este archivo es la configuración de Tailwind CSS para el proyecto.
// Tailwind CSS es un framework de CSS que permite crear diseños personalizados rápidamente.